# Funky demo

## Dependency management

Run `uv sync`.

## Run demo

1. Create `.env` and populate the api keys.
2. Run `adk web --session_service_uri=memory:/`. Use in-memory session service for better agent-swarm performance.
